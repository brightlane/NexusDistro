name: Vulture 10K Engine

on:
  schedule:
    - cron: '0 4 * * *'
  workflow_dispatch: 

jobs:
  build_network:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: Run Core Engine
        run: |
          # The -f check ensures the action doesn't fail if a file is missing
          if [ -f vulture_gen.py ]; then python vulture_gen.py; else echo "vulture_gen.py not found"; fi
          if [ -f social_gen.py ]; then python social_gen.py; else echo "social_gen.py not found"; fi
          if [ -f ai_feed_gen.py ]; then python ai_feed_gen.py; else echo "ai_feed_gen.py not found"; fi

      - name: Deploy Changes
        run: |
          git config --global user.name "Vulture-Bot"
          git config --global user.email "engine@nexusdistro.io"
          git add .
          if git diff --staged --quiet; then
            echo "No changes to commit"
          else
            git commit -m "Vulture 10K Refresh: $(date)"
            git push
          fi
